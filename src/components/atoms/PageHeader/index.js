import React from "react";
import Typography from "@mui/material/Typography";

const PageHeader = ({ title }) => {
  return <Typography className="pageHeader">{title}</Typography>;
};

export default PageHeader;
